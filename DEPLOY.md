# Инструкция по деплою бота на облачные платформы

## Вариант 1: Railway (Рекомендуется) 🚂

Railway - простая платформа с бесплатным тарифом ($5 кредитов в месяц).

### Шаги:

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий с ботом

3. **Настройте переменные окружения:**
   - В настройках проекта найдите "Variables"
   - Добавьте переменную: `BOT_TOKEN` = ваш токен бота

4. **Запустите деплой:**
   - Railway автоматически определит Python проект
   - Используйте команду: `python bot.py`
   - Или создайте Procfile (уже создан)

5. **Готово!**
   - Бот будет работать 24/7
   - Логи доступны в разделе "Deployments"

---

## Вариант 2: Render 🌐

Render - еще одна простая платформа с бесплатным тарифом.

### Шаги:

1. **Зарегистрируйтесь на Render:**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

3. **Настройте сервис:**
   - **Name:** changesbot (или любое имя)
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot.py`

4. **Добавьте переменные окружения:**
   - В разделе "Environment Variables"
   - Добавьте: `BOT_TOKEN` = ваш токен бота

5. **Запустите деплой:**
   - Нажмите "Create Web Service"
   - Дождитесь завершения деплоя

---

## Вариант 3: Fly.io ✈️

Fly.io - современная платформа с хорошим бесплатным тарифом.

### Шаги:

1. **Установите Fly CLI:**
   ```bash
   # Windows (PowerShell)
   iwr https://fly.io/install.ps1 -useb | iex
   ```

2. **Зарегистрируйтесь:**
   ```bash
   fly auth signup
   ```

3. **Создайте приложение:**
   ```bash
   fly launch
   ```

4. **Настройте переменные:**
   ```bash
   fly secrets set BOT_TOKEN=ваш_токен_бота
   ```

5. **Запустите:**
   ```bash
   fly deploy
   ```

---

## Вариант 4: VPS (Виртуальный сервер) 🖥️

Если у вас есть VPS (например, Timeweb, DigitalOcean, Hetzner):

### Шаги:

1. **Подключитесь к серверу по SSH**

2. **Установите Python и зависимости:**
   ```bash
   sudo apt update
   sudo apt install python3 python3-pip git
   ```

3. **Клонируйте репозиторий:**
   ```bash
   git clone <ваш_репозиторий>
   cd changesbot
   ```

4. **Установите зависимости:**
   ```bash
   pip3 install -r requirements.txt
   ```

5. **Создайте файл .env:**
   ```bash
   nano .env
   # Добавьте: BOT_TOKEN=ваш_токен
   ```

6. **Используйте systemd для автозапуска:**
   
   Создайте файл `/etc/systemd/system/changesbot.service`:
   ```ini
   [Unit]
   Description=Telegram Changes Bot
   After=network.target

   [Service]
   Type=simple
   User=ваш_пользователь
   WorkingDirectory=/путь/к/changesbot
   ExecStart=/usr/bin/python3 /путь/к/changesbot/bot.py
   Restart=always
   RestartSec=10

   [Install]
   WantedBy=multi-user.target
   ```

7. **Запустите сервис:**
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable changesbot
   sudo systemctl start changesbot
   ```

8. **Проверьте статус:**
   ```bash
   sudo systemctl status changesbot
   ```

---

## Рекомендации

- **Для начинающих:** Railway или Render (самые простые)
- **Для продвинутых:** VPS с systemd (полный контроль)
- **Для тестирования:** Fly.io (быстрый деплой)

## Важно!

⚠️ **Не загружайте файл `.env` в Git!** Он содержит секретные данные.
- Убедитесь, что `.env` в `.gitignore`
- Используйте переменные окружения на платформе

## Проверка работы

После деплоя:
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Если бот отвечает - всё работает! ✅


